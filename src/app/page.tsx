import { PageIntro } from "@/components/page-intro";

export default function Home() {
  return (
    <PageIntro
      eyebrow="Research report"
      title="Which integrations can become agent tools today?"
      summary="A verified look at access, authentication, API breadth, MCP availability, and the blockers that matter across 100 requested apps."
    />
  );
}
